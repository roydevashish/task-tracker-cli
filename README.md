# Task Tracker CLI

Task Tracker CLI is a lightweight command-line backend application for managing tasks efficiently. It allows users to add, update, delete, and track tasks directly from the terminal, with support for task statuses like todo, in-progress, and done.